1. Install node.js dulu
2. cd ke folder ini lalu ketikkan perintah "npm install" untuk menginstall semua dependencies
3. lalu bundle assets ke production mode dengan perintah "npm run build"
4. jalankan aplikasi dengan live server pada file ./dist/index.html atau taruh di web server